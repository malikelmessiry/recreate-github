import './LoginControls.css';

const LoginControls = () => {
    return (
        <section>
            <p>Sign in</p>
            <p className="sign-up">Sign up</p>
        </section>
    );
}

export default LoginControls;